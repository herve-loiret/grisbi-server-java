    <Account
        Name="${name}"
        Id="${id}"
        Number="${number}"
        Owner="${owner}"
        Kind="${kind}"
        Currency="${currency}"
        Path_icon="${pathIcon}"
        Bank="${bank}"
        Bank_branch_code="${bankBranchCode}"
        Bank_account_number="${bankAccountNumber}"
        Key="${key}"
        Bank_account_IBAN="${bankAccountIBAN}"
        Initial_balance="${initialBalance}"
        Minimum_wanted_balance="${minimumWantedBalance}"
        Minimum_authorised_balance="${minimumAuthorisedBalance}"
        Closed_account="${closedAccount}"
        Show_marked="${showMarked}"
        Show_archives_lines="${show_archives_lines}"
        Lines_per_transaction="${linesPerTransaction}"
        Comment="${comment}"
        Owner_address="${ownerAddress}"
        Default_debit_method="${defaultDebitMethod}"
        Default_credit_method="${defaultCreditMethod}"
        Sort_by_method="${sortByMethod}"
        Neutrals_inside_method="${neutralsInsideMethod}"
        Sort_order="${sortOrder}"
        Ascending_sort="${ascendingSort}"
        Column_sort="${columnSort}"
        Sorting_kind_column="${sortingKindColumn}"
        Form_columns_number="${formColumnsNumber}"
        Form_lines_number="${formLinesNumber}"
        Form_organization="${formOrganization}"
        Form_columns_width="${formColumnsWidth}"
        Bet_use_budget="1"
        Bet_credit_card="0"
        Bet_start_date="02/01/2013"
        Bet_months=""
        Bet_UT=""
        Bet_auto_inc_month=""
        Bet_select_transaction_label="
        Bet_select_scheduled_label=""
        Bet_select_futur_label=""
        Bet_SD=""
        Bet_Fi="" />